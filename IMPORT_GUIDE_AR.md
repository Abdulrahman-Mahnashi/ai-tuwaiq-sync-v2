# 🚀 دليل رفع ملفات JSON - دليل شامل

## 📋 نظرة عامة

لديك **70 ملف JSON** تريد رفعها إلى قاعدة البيانات. يمكنك استخدام إحدى الطريقتين:

---

## 🌐 الطريقة 1: استخدام الواجهة (الأسهل)

### الخطوات:

1. **افتح التطبيق**
   ```bash
   npm run dev
   ```

2. **اذهب إلى تبويب "رفع الملفات"**
   - ستجد زر "اختر ملفات JSON"
   - يمكنك اختيار ملفات متعددة في مرة واحدة

3. **اختر جميع ملفات JSON**
   - اضغط `Ctrl` (أو `Cmd` على Mac) واختر الملفات
   - أو اختر مجلد كامل إذا كان المتصفح يدعم ذلك

4. **اضغط "رفع الملفات"**
   - سترى شريط تقدم
   - ستحصل على تقرير بالنتائج

### ✅ المميزات:
- سهل الاستخدام
- لا يحتاج سطر أوامر
- تقارير فورية
- معالجة مرئية

---

## 💻 الطريقة 2: استخدام Script (الأسرع للملفات الكثيرة)

### الخطوات:

1. **ضع ملفات JSON في مجلد**
   ```bash
   # أنشئ مجلد (إذا لم يكن موجوداً)
   mkdir -p data/json
   
   # ضع جميع ملفات JSON في data/json/
   # يجب أن تكون جميع الملفات في هذا المجلد
   ```

2. **تأكد من وجود ملف `.env`**
   ```env
   VITE_SUPABASE_URL=your_supabase_url
   VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
   ```
   
   **ملاحظة**: إذا كان ملف `.env` موجوداً في المجلد الرئيسي، سيتم استخدامه تلقائياً.

3. **ثبت الحزم** (إذا لم تكن مثبتة)
   ```bash
   npm install
   ```

4. **شغّل Script الرفع**
   ```bash
   # إذا كانت الملفات في data/json
   npm run import:json:data
   
   # أو حدد مسار مخصص
   npm run import:json ./path/to/your/json/files
   ```

5. **انتظر النتائج**
   - سترى تقريراً مفصلاً
   - عدد الملفات المعالجة
   - عدد المشاريع المرفوعة
   - الأخطاء (إن وجدت)

### ✅ المميزات:
- أسرع للملفات الكثيرة (70 ملف)
- تقرير مفصل
- معالجة تلقائية للأخطاء
- تجنب التكرار

---

## 📁 هيكل المجلد الموصى به

```
ai-tuwaiq-sync/
├── data/
│   └── json/
│       ├── bootcamp1_projects.json
│       ├── bootcamp2_projects.json
│       ├── bootcamp3_projects.json
│       └── ... (70 ملف)
├── scripts/
│   └── import-json-to-db.js
└── .env
```

---

## 📊 مثال على النتائج المتوقعة

بعد تشغيل Script، سترى شيء مثل:

```
📁 Reading JSON files from: /path/to/data/json

✅ Found 70 JSON files

📄 Processing: bootcamp1_projects.json...
   📦 Found 15 project(s)
   ✅ Completed: bootcamp1_projects.json

📄 Processing: bootcamp2_projects.json...
   📦 Found 12 project(s)
   ✅ Completed: bootcamp2_projects.json

...

==================================================
📊 IMPORT SUMMARY
==================================================
✅ Successfully imported: 1050 projects
❌ Failed imports: 5
📁 Total files processed: 70
📦 Total projects found: 1055
==================================================
```

---

## 🔧 حل المشاكل الشائعة

### ❌ خطأ: Missing Supabase credentials
**الحل**: 
- تأكد من وجود ملف `.env` في المجلد الرئيسي
- تأكد من صحة البيانات (URL و Key)

### ❌ خطأ: No JSON files found
**الحل**:
- تأكد من أن الملفات موجودة في المجلد المحدد
- تأكد من أن الملفات بصيغة `.json`
- تحقق من المسار الصحيح

### ❌ خطأ: Invalid JSON
**الحل**:
- تحقق من أن ملفات JSON صحيحة التنسيق
- استخدم JSON validator للتحقق
- راجع الأخطاء في التقرير

### ❌ خطأ: Database connection failed
**الحل**:
- تحقق من اتصال الإنترنت
- تحقق من صحة مفاتيح API
- تأكد من أن Supabase يعمل

---

## 💡 نصائح مهمة

1. **النسخ الاحتياطي**: احتفظ بنسخة من ملفات JSON الأصلية
2. **الاختبار**: جرب رفع ملف واحد أولاً للتأكد من أن كل شيء يعمل
3. **التقارير**: راجع التقارير للتأكد من نجاح الرفع
4. **التكرار**: النظام يتجنب إضافة مشاريع مكررة تلقائياً

---

## 🎯 الخطوات السريعة (Quick Start)

```bash
# 1. ضع ملفات JSON في data/json
# 2. تأكد من وجود .env
# 3. شغّل الأمر:
npm run import:json:data

# 4. انتظر النتائج
# 5. افتح التطبيق للتحقق من البيانات
```

---

## 📞 المساعدة

إذا واجهت أي مشاكل:
1. راجع ملف `IMPORT_JSON_FILES.md` للتفاصيل الكاملة
2. تحقق من الأخطاء في التقرير
3. تأكد من أن قاعدة البيانات جاهزة (راجع `DATABASE_SETUP.md`)

---

**بالتوفيق! 🚀**

