# دليل البدء السريع | Quick Start Guide

## 🚀 الخطوات السريعة

### 1. إعداد قاعدة البيانات

```bash
# 1. افتح Supabase Dashboard
# 2. اذهب إلى SQL Editor
# 3. انسخ محتوى ملف: supabase/migrations/20240101000000_create_projects_table.sql
# 4. قم بتشغيله
# 5. انسخ محتوى ملف: supabase/seed.sql
# 6. قم بتشغيله لإضافة بيانات تجريبية
```

### 2. إعداد متغيرات البيئة

أنشئ ملف `.env` في مجلد `ai-tuwaiq-sync`:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key

# OpenAI API Key (اختياري - لتفعيل AI Agent)
# إذا لم يتم توفيره، سيستخدم النظام خوارزمية بسيطة
VITE_OPENAI_API_KEY=your_openai_api_key_here
```

**ملاحظة:** إذا لم تقم بإضافة `VITE_OPENAI_API_KEY`، سيعمل النظام باستخدام خوارزمية بسيطة لحساب التشابه (Fallback تلقائي).

### 3. تشغيل التطبيق

```bash
npm install
npm run dev
```

## ✨ الميزات الجديدة

### ✅ نظام قاعدة البيانات
- جميع المشاريع مخزنة في قاعدة بيانات Supabase
- لا حاجة لرفع ملفات يدوياً

### ✅ AI Agent للمقارنة التلقائية
- اكتب فكرة مشروعك
- الـ AI Agent يقارنها تلقائياً مع المشاريع الموجودة باستخدام GPT-4o-mini
- يعرض النتائج المشابهة مع درجات التشابه وأسباب المقارنة
- **Fallback تلقائي:** إذا لم يكن OpenAI API متوفراً، يستخدم خوارزمية بسيطة

### ✅ واجهة محسّنة
- تبويب "تحليل الفكرة" لإدخال الأفكار الجديدة
- تبويب "المشاريع" لعرض جميع المشاريع من قاعدة البيانات
- عرض المشاريع المشابهة مع تفاصيل كاملة

## 📝 كيفية الاستخدام

1. افتح التطبيق
2. اذهب إلى تبويب **"تحليل الفكرة"**
3. اكتب فكرة مشروعك (مثال: "أريد إنشاء تطبيق للتعلم الإلكتروني")
4. اضغط على **"تحليل الفكرة"**
5. انتظر قليلاً حتى يقوم الـ Agent بالمقارنة
6. ستظهر النتائج المشابهة مع درجات التشابه

## 🎯 مثال على الاستخدام

**الفكرة المدخلة:**
```
أريد إنشاء تطبيق للتعلم الإلكتروني باستخدام React و Node.js مع ميزات الفيديو والاختبارات
```

**النتائج:**
- تطبيق التعلم الإلكتروني (85% تشابه)
- نظام إدارة المشاريع (45% تشابه)
- تطبيق التجارة الإلكترونية (30% تشابه)

## ⚙️ الإعدادات المتقدمة

لمزيد من التفاصيل، راجع ملف `DATABASE_SETUP.md`

---

**Happy Coding! 🎉**

