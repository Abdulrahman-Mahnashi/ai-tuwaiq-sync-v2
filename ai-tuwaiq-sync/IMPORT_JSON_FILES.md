# دليل استخدام ملفات JSON

## ✅ تشغيل بدون قاعدة بيانات (الوضع المحلي المقترح)

استخدم هذه الطريقة إذا أردت تشغيل الموقع واعتماد المقارنة على ملف محلي واحد دون الحاجة إلى Supabase.

### الخطوات:

1. **ضع كل ملفات JSON في مجلد واحد**  
   ```bash
   mkdir -p data/json
   # انسخ هنا ملفاتك الـ 70 (أو أي عدد) بصيغة JSON
   ```

2. **ادمج الملفات في ملف واحد**  
   ```bash
   npm install   # في حال لم تكن الحزم مثبتة
   npm run combine:json
   ```
   هذا الأمر سينشئ الملف `public/data/projects-local.json` الذي يستخدمه الموقع تلقائياً.

3. **شغّل الموقع**  
   ```bash
   npm run dev
   ```
   - عند فتح تبويب "تحليل الفكرة" سيقرأ التطبيق البيانات من الملف `projects-local.json`
   - تبويب "المشاريع" سيعرض نفس البيانات بدون الحاجة لقاعدة بيانات

### ملاحظات:
- يمكنك إعادة تشغيل الأمر `npm run combine:json` كلما أضفت ملفات جديدة.
- تأكد من وجود الملف الناتج داخل `public/data/` لأن التطبيق يعتمد عليه تلقائياً.
- إذا أردت تحديث البيانات بدون إعادة تشغيل التطبيق، يكفي إعادة دمج الملفات وسيقرأ المتصفح الملف المحدّث عند التحديث.

---

# (اختياري) رفع الملفات إلى قاعدة البيانات | Import JSON Files Guide

## 📁 طريقة 1: استخدام Script Node.js (موصى به للملفات الكثيرة)

### الخطوات:

1. **ضع ملفات JSON في مجلد**
   ```bash
   # أنشئ مجلد للملفات
   mkdir -p data/json
   
   # ضع جميع ملفات JSON في هذا المجلد
   # data/json/
   #   ├── bootcamp1_projects.json
   #   ├── bootcamp2_projects.json
   #   └── ...
   ```

2. **ثبت الحزم المطلوبة**
   ```bash
   npm install
   ```

3. **تأكد من وجود ملف `.env`**
   ```env
   VITE_SUPABASE_URL=your_supabase_url
   VITE_SUPABASE_PUBLISHABLE_KEY=your_supabase_anon_key
   ```

4. **شغّل Script الرفع**
   ```bash
   # رفع من مجلد محدد
   npm run import:json ./data/json
   
   # أو إذا كانت الملفات في data/json
   npm run import:json:data
   ```

### الميزات:
- ✅ رفع ملفات متعددة في مرة واحدة
- ✅ معالجة تلقائية لأشكال JSON المختلفة
- ✅ تجنب التكرار (لا يضيف مشاريع مكررة)
- ✅ تقارير مفصلة عن النتائج
- ✅ معالجة الأخطاء

---

## 🌐 طريقة 2: استخدام الواجهة (للرفع من المتصفح)

### الخطوات:

1. **افتح التطبيق**
   ```bash
   npm run dev
   ```

2. **اذهب إلى تبويب "رفع الملفات"**

3. **اختر ملفات JSON**
   - يمكنك اختيار ملفات متعددة
   - اضغط على "اختر ملفات JSON"
   - حدد جميع الملفات التي تريد رفعها

4. **اضغط على "رفع الملفات"**
   - سيقوم النظام بمعالجة جميع الملفات
   - سترى شريط تقدم
   - ستحصل على تقرير بالنتائج

### الميزات:
- ✅ سهل الاستخدام
- ✅ رفع من المتصفح مباشرة
- ✅ شريط تقدم مرئي
- ✅ تقارير فورية

---

## 📋 هيكل ملف JSON المدعوم

يدعم Script أشكال JSON مختلفة:

### الشكل 1: مصفوفة مباشرة
```json
[
  {
    "title": "اسم المشروع",
    "description": "وصف المشروع",
    "bootcamp": "اسم البرنامج",
    "technologies": ["React", "Node.js"],
    "team_members": ["عضو 1", "عضو 2"],
    "status": "completed"
  }
]
```

### الشكل 2: كائن يحتوي على projects
```json
{
  "projects": [
    {
      "title": "اسم المشروع",
      "description": "وصف المشروع"
    }
  ]
}
```

### الشكل 3: كائن يحتوي على data
```json
{
  "data": [
    {
      "title": "اسم المشروع",
      "description": "وصف المشروع"
    }
  ]
}
```

### الشكل 4: كائن واحد
```json
{
  "title": "اسم المشروع",
  "description": "وصف المشروع",
  "bootcamp": "اسم البرنامج"
}
```

## 🔄 أسماء الحقول المدعومة

يقبل Script أسماء حقول مختلفة:

- **العنوان**: `title`, `name`, `project_title`
- **الوصف**: `description`, `desc`, `project_description`
- **البرنامج**: `bootcamp`, `bootcamp_name`, `program`
- **التقنيات**: `technologies`, `tech`, `tech_stack`
- **الفريق**: `team_members`, `team`, `members`
- **الحالة**: `status`

## 📊 تقرير الرفع

بعد الانتهاء، ستحصل على تقرير يحتوي على:
- عدد الملفات المعالجة
- عدد المشاريع المرفوعة بنجاح
- عدد المشاريع التي فشل رفعها
- قائمة بالأخطاء (إن وجدت)

## ⚠️ ملاحظات مهمة

1. **التكرار**: النظام يتجنب إضافة مشاريع مكررة (بناءً على العنوان)
2. **الأخطاء**: إذا فشل رفع مشروع معين، سيستمر النظام في معالجة البقية
3. **البيانات**: تأكد من أن بيانات JSON صحيحة التنسيق
4. **الحجم**: لا توجد حدود صارمة، لكن الملفات الكبيرة قد تحتاج وقت أطول

## 🚀 مثال سريع

```bash
# 1. ضع ملفات JSON في data/json
mkdir -p data/json
# انسخ ملفاتك هنا

# 2. شغّل Script
npm run import:json:data

# 3. انتظر النتائج
# ✅ Successfully imported: 150 projects
# ❌ Failed imports: 2
# 📁 Total files processed: 70
```

## 🆘 حل المشاكل

### خطأ: Missing Supabase credentials
**الحل**: تأكد من وجود ملف `.env` مع البيانات الصحيحة

### خطأ: No JSON files found
**الحل**: تأكد من أن الملفات موجودة في المجلد المحدد وأنها بصيغة `.json`

### خطأ: Invalid JSON
**الحل**: تحقق من أن ملفات JSON صحيحة التنسيق

### خطأ: Database connection failed
**الحل**: تحقق من اتصال قاعدة البيانات ومفاتيح API

---

**للمزيد من المساعدة، راجع ملف `DATABASE_SETUP.md`**

